# Twilio credentials (server-side only — not exposed to the browser)
TWILIO_PHONE_NUMBER=+17655550123
TWILIO_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token

# Optional: SMS server port (default 3000). Vite proxies /api to this port.
# SMS_PORT=3000
